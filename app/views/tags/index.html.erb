<h1>All Tags</h1>

<ul>
  <% @tags.each do |t| %>
    <li>
      <%= link_to "#{t.name} (#{t.articles.size})", tag_path(t) %>
      <%= link_to "Delete", tag_path(t), method: :delete, confirm: "Really delete this tag?" %>
    </li>
  <% end %>
</ul>

