<h1> All articles tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |a| %>
    <li>
      <%= link_to a.title, article_path(a) %>
    </li>
  <% end %>
</ul>

<br>
<br>

<nav>
  <%= link_to "All tags", tags_index_path %>
</nav>